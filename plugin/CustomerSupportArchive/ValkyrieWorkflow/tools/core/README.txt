CORE TOOLS:

These are basic functions which have no dependencies on packages 
not distributed in the standard python environment (e.g. need apt-get or pip)

These functions must be written for maximum compatibility.  Specifically, 
they will maintain both python2 and python3 compatibility.
