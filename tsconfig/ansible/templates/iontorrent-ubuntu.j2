# {{ ansible_managed }}
# To change the server location, edit /usr/share/ion-tsconfig/ansible/group_vars/all_local
# and change the value of keyword 'ion_apt_server'
deb [arch=amd64] http://{{ubuntu_apt_server}}/ubuntu/ {{ansible_distribution_release}} main restricted
deb [arch=amd64] http://{{ubuntu_apt_server}}/ubuntu/ {{ansible_distribution_release}} universe
deb [arch=amd64] http://{{ubuntu_apt_server}}/ubuntu/ {{ansible_distribution_release}}-security main restricted
deb [arch=amd64] http://{{ubuntu_apt_server}}/ubuntu/ {{ansible_distribution_release}}-security universe
deb [arch=amd64] http://{{ubuntu_apt_server}}/ubuntu/ {{ansible_distribution_release}}-updates main restricted
deb [arch=amd64] http://{{ubuntu_apt_server}}/ubuntu/ {{ansible_distribution_release}}-updates universe
